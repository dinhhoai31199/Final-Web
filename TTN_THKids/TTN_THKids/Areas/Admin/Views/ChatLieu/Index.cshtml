@model IEnumerable<TTN_THKids.Models.ChatLieu>

@{
    ViewBag.Title = "Index";
}

<h2>Danh sách chất liệu </h2>

<p>
    @Html.ActionLink("Tạo mới", "Create")
</p>
<table class="table table-bordered table-hover" >
    <tr>
        <th>
            Tên chất liệu
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenChatLieu)
        </td>
        <td>
            <a href="~/Admin/ChatLieu/Edit/@item.MaChatLieu"> Chỉnh sửa |</a>
            <a href="~/Admin/ChatLieu/Details/@item.MaChatLieu"> Chi tiết |</a>
            <a href="~/Admin/ChatLieu/Delete/@item.MaChatLieu"> Xóa</a>
            @*@Html.ActionLink("Chỉnh sửa", "Edit", new { id=item.MaChatLieu }) |
        @Html.ActionLink("Chi tiết", "Details", new { id=item.MaChatLieu }) |
        @Html.ActionLink("Xóa", "Delete", new { id=item.MaChatLieu })*@
        </td>
    </tr>
}

</table>
