@model IEnumerable<TTN_THKids.Models.DanhMucSanPham>

@{
    ViewBag.Title = "Index";
}

<h2>Danh sách danh mục sản phẩm</h2>

<p>
    @Html.ActionLink("Tạo mới", "Create")
</p>
<table class="table table-bordered table-hover">
    <tr>
        <th>
            Mã danh mục sản phẩm
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.TenDMSP)*@
            Tên danh mục sp
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DanhMuc.TenDanhMuc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TenDMSP)
        </td>
        <td>
            <a href="~/Admin/DanhMucSanPham/Edit/@item.MaDMSP"> Chỉnh sửa |</a>
            <a href="~/Admin/DanhMucSanPham/Details/@item.MaDMSP"> Chi tiết |</a>
            <a href="~/Admin/DanhMucSanPham/Delete/@item.MaDMSP"> Xóa</a>

            @*@Html.ActionLink("Chỉnh sửa", "Edit", new { id=item.MaDMSP }) |
        @Html.ActionLink("Chi tiết", "Details", new { id=item.MaDMSP }) |
        @Html.ActionLink("Xóa", "Delete", new { id=item.MaDMSP })*@
        </td>
    </tr>
}

</table>
