@model IEnumerable<TTN_THKids.Models.DanhMuc>

@{
    ViewBag.Title = "Index";
}

<h2>Danh sách danh mục</h2>

<p>
    @Html.ActionLink("Tạo mới", "Create")
</p>
<table class="table table-bordered table-hover">
    <tr>
        <th>
            Tên danh mục
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenDanhMuc)
        </td>
        <td>
            <a href="~/Admin/DanhMuc/Edit/@item.MaDanhMuc"> Chỉnh sửa |</a>
            <a href="~/Admin/DanhMuc/Details/@item.MaDanhMuc"> Chi tiết |</a>
            <a href="~/Admin/DanhMuc/Delete/@item.MaDanhMuc"> Xóa</a>

            @*@Html.ActionLink("Chỉnh sửa", "Edit", new { id=item.MaDanhMuc }) |
        @Html.ActionLink("Chi tiết", "Details", new { id=item.MaDanhMuc }) |
        @Html.ActionLink("Xóa", "Delete", new { id=item.MaDanhMuc })*@
        </td>
    </tr>
}

</table>
